# GUI dependencies — install alongside the root requirements.txt
#   pip install -r requirements.txt -r gui/requirements.txt

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Included with FastAPI but pinned for clarity
pydantic>=2.0.0
